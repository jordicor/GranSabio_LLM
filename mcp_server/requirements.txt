# Gran Sabio LLM MCP Server Dependencies
# Install with: pip install -r requirements.txt

# MCP SDK - Model Context Protocol
mcp>=1.0.0

# HTTP client for API calls
httpx>=0.27.0

# Note: These are the minimal dependencies for the MCP server.
# The main Gran Sabio LLM application has its own requirements.txt
# in the project root.
